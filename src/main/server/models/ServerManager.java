package main.server.models;

import main.server.models.persistents.DBManager;

/**
 * Project Name: HangmanServer
 */

public class ServerManager {
    public ServerManager(DBManager dbManager) {
    }
}
